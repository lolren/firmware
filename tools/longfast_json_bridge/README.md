# Legacy Path (Compatibility)

The primary PC webserver files were moved to:

- `longfast_json_bridge/pc_webserver/`

This folder keeps compatibility helpers:

- `longfast_json_bridge_web.py`: launcher shim that forwards to the new location
- `make_explainer_video.sh`: explainer-video generator script
