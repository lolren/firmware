# LongFast JSON Bridge Explainer Video

This repository includes a generated explainer video:

- `docs/assets/longfast-json-bridge-explainer.mp4`

## Regenerate the video

```bash
cd /home/lolren/Desktop/Meshtastic_bot
tools/longfast_json_bridge/make_explainer_video.sh
```

## Source script

- `tools/longfast_json_bridge/make_explainer_video.sh`

## Voiceover Script (Optional)

Use this narration when recording a spoken version:

1. `LongFast JSON Bridge extends Meshtastic firmware with JSON endpoints for channel and private messaging.`
2. `It includes JSON read and write endpoints for chat history and message sending.`
3. `It also adds JSON node configuration for long name, short name, and Wi-Fi settings.`
4. `A local Python web app provides a ready-to-use browser interface and right-click DM actions.`
5. `All steps to build, flash, and run are documented so other users can replicate the setup.`
